export const environment = {
  production: true,
  useHash: true,
  baseHref: '/ecommerce-githubpages/', 
  apiUrl: '' // Em produção no GH Pages, geralmente fica vazio ou com a URL da API real
};